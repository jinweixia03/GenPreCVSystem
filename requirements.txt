# GenPreCVSystem Python 依赖
# 安装方式: pip install -r requirements.txt

# ============ 核心依赖 (必需) ============
# YOLO 推理服务
ultralytics>=8.0.0

# PyTorch - 根据需求选择 CPU/GPU 版本
# CPU 版本 (默认):
torch>=2.0.0
torchvision>=0.15.0
# GPU 版本 (CUDA 12.4)，使用：
# pip install torch==2.5.0 torchvision==0.20.0 torchaudio==2.5.0 --index-url https://download.pytorch.org/whl/cu124

# ============ 基础依赖 ============
numpy>=1.24.0
pillow>=9.0.0

# ============ 小样本学习 (可选) ============
# 仅用于遥感影像小样本分类功能
easyfsl>=1.3.0

# ============ 图像处理 (可选但推荐) ============
opencv-python>=4.8.0

# ============ 工具库 ============
tqdm>=4.65.0
matplotlib>=3.7.0
